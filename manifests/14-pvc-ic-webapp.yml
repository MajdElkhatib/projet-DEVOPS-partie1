apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-ic-webapp
  namespace: icgroup
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "longhorn"
  resources:
    requests:
      storage: 2Gi

